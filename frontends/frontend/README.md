# Onchain-Proc Dashboard

The official frontend interface for the Onchain-Proc platform. Built with React and Vite.

## ⚡ Features

- Connect Wallet functionality
- Smart Contract interaction forms
- Real-time data visualization
- Responsive design

## 🛠 Installation

1. **Clone the repository** (if you haven't already)
2. **Navigate** to this folder:
   ```bash
   cd frontends/frontend
   ```
3. **Install dependencies**:
   ```bash
   npm install
   ```

## 💻 Usage

Start the development server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```

## 🔧 Configuration

Copy `.env.example` to `.env` (if available) to configure your specific environment variables, such as RPC URLs or contract addresses.

## 🤝 Contributing

Pull requests are welcome. For major changes, please open an issue first to discuss what you would like to change.
